class Solution {
  public:
    int findElementAtIndex(int n, int key, vector<int> &arr) {
        for(int i=0;i<n;i++){
            if(arr[i]==key){
                
            }
        }
        return arr[key];
    }
};
